# Requirements pour les pré-calculs (Epic 1)
# Python 3.12, Conda (paris_risques)

# Configuration
pyyaml>=6.0

# Géospatial
geopandas>=0.13.0
osmnx>=1.6.0  # Pour pré-calculs (casernes/hôpitaux, microzones)

# Données
pandas>=2.0.0
numpy>=1.24.0

# Machine Learning (pour clustering IRIS)
scikit-learn>=1.3.0

# Validation patterns (Story 1.4)
pydantic>=2.0

# Tests (optionnel pour pré-calculs)
pytest>=7.4.0
